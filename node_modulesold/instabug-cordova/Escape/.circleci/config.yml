version: 2
jobs:
  test:
    docker:
      - image: swift:4.2
    working_directory: /Users/distiller/project/
    steps:
      - checkout
      - run:
          name: Build
          command: swift build
      - run:
          name: Test
          command: swift test

workflows:
  version: 2
  test:
    jobs:
      - test
